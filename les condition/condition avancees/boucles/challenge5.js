const prompt = require('prompt-sync')();

let n = parseFloat(prompt("Entrer un nonbre : "));
let x = parseFloat(prompt("Entrer un exeposant : "));

let r = 1 ;
for (let i = 1 ; i <= n ; i++ ) {
    console.log(i)
}
